apiVersion: v1
kind: Service
metadata:
  name: citesting-service
spec:
  selector:
    app: citesting-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30008